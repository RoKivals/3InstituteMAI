#include "header.h"

int main() {
    // Создаём матрицу (массив точек), auto автоматически определяет тип
    // переменной Используется, чтобы не писать громоздкую запись вложенных
    // векторов
    auto dots = create_matrix();

    return 0;
}
